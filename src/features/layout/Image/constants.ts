export enum SIZE {
  XS = 420,
  SM = 640,
  MD = 768,
  LG = 1024,
  XL = 1280,
}

export const SIZE_BREAKPOINTS = [SIZE.XS, SIZE.SM, SIZE.MD, SIZE.LG, SIZE.XL];

export enum SourceFormatType {
  jpg = 'jpg',
  webp = 'webp',
}
