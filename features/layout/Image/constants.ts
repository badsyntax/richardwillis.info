export enum SIZE {
  SM = 200,
  MD = 725,
  LG = 1075,
  XL = 1280,
}

export const SIZE_BREAKPOINTS = [SIZE.SM, SIZE.MD, SIZE.LG, SIZE.XL];

export enum SourceFormatType {
  jpg = 'jpg',
  webp = 'webp',
}
